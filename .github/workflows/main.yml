name: show-directory
on: push
jobs:
  runs-on: ubuntu-latest
    
   steps:
      - uses: actions/checkout@v4
      - run: make setup
      - run: npm install
      - run: npm run lint
      - name: run tests
        run: npm test
